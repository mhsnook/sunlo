# learner updates their profile

learner:

- login
  // STUB — ported from e2e/mutations/profile.spec.ts (test.skip). To implement:
  // - open profile settings
  // - update the username (and/or other fields)
  // - verify the change persisted
  // DB-state agreement belongs in inline serverChecks on myProfileCollection,
  // not in this scene.

# learner uploads an avatar image

learner:

- login
  // STUB — ported from e2e/mutations/profile.spec.ts (test.skip). To implement:
  // - open the avatar editor
  // - upload an image
  // - verify the avatar_path is stored

# learner changes their email address

learner:

- login
  // STUB — ported from e2e/mutations/profile.spec.ts (test.skip). To implement:
  // - open the email-change form
  // - submit a new email
  // - verify a confirmation step is triggered

# learner changes their password

learner:

- login
  // STUB — ported from e2e/mutations/profile.spec.ts (test.skip). To implement:
  // - open the password-change form
  // - submit a new password
  // - verify login works with the new password
