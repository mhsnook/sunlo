# Implementation Plans

Generated by the improve skill on 2026-07-08, against commit `1c4b60ef`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                 | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Implement the 9 stubbed social & profile scene specs                  | P1       | M      | —          | TODO   |
| 002  | Friend-request RPC returning new relationship state (migration track) | P1       | M      | 001        | TODO   |
| 003  | Migrate deprecated useMutation sites (notifications, playlists, deck) | P2       | L      | —          | TODO   |
| 004  | Add beforeLoad guard to /admin route tree                             | P2       | S      | —          | TODO   |
| 005  | Extract shared upvote action factory                                  | P3       | S      | —          | TODO   |
| 006  | Add pre-commit secret scanning                                        | P2       | S      | —          | TODO   |
| 007  | Extract review session-setup into feature hooks                       | P3       | M      | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001**: the scene specs are characterization coverage for the friend-request flows; the rewrite must land under a passing net. 001 may pin the mutual-invite toast quirk with a comment; 002 removes the pin.
- **002 is migration-track**: it adds a Postgres function, so per CLAUDE.md it ships via a `next-<version>` branch (`next-0.29` at planning time), not `main`. All other plans are fast-track (`main`).
- **003 and 002 must not run concurrently** — 003 explicitly excludes `src/features/social/` because 002 owns it, but both touch mutation-pattern conventions; land 002 first if possible.
- **005 and 003 are disjoint by design**: 005 owns the three upvote buttons; 003 explicitly leaves them alone.
- **007 should not run concurrently with anything else touching review.**

## Findings considered and rejected

Recorded so they are not re-audited:

- `manifest!` non-null assertion in `useNextValid` (`features/review/hooks.ts:168`): unreachable in practice — the only call path is guarded by `$lang.review.go.tsx:56` (`if (!day?.manifest?.length) return <Navigate/>`). Defensive `?? []` welcome if touching the file, not worth a plan.
- Swallowed catch in `usePostReview` onSuccess (`features/review/hooks.ts:538`): deliberate and documented inline — isolates local-sync failures from a misleading "error posting review" toast. By design.
- `createOptimisticAction.onMutate` blocks "missing try/catch" (comment-dialog): that IS the documented TanStack DB pattern — `mutationFn` throwing rolls back `onMutate` writes automatically.
- "Realtime channel leak on userId change" (`useSocialRealtime`): wrong — the early return precedes channel creation, so there is nothing to clean up; standard effect pattern.
- Missing zero-row guard on `messageTagsCollection.onInsert`: intentional asymmetry — INSERT raises a PostgREST error under RLS denial; only UPDATE/DELETE silently affect 0 rows (the guard exists there, with a comment).
- "Review mutations bypass collection handlers": CLAUDE.md explicitly exempts FSRS review submission (server-side transformation unpredictable client-side); review syncs via `writeInsert/writeUpdate` of returned rows, which is the endorsed alternative.
- "Chat feature lacks collections/scenetest": `src/features/chat/` is the AI phrase-discovery chat (Edge Function semantic search, documented mock fallback); ephemeral zustand state is the right shape. User-to-user chat lives in `social/`.
- Toast imports inside feature hooks ("layering violation"): established repo convention; migration churn exceeds value.
- `ensureManifestCardsInCollection` full refetch (`features/review/hooks.ts:263`): justified self-heal with an inline comment, fires only when drift is detected. A targeted `.in('phrase_id', missing)` fetch is a nice-to-have if ever touching that code.
- Commented-out `onSettled` refetch (`features/social/hooks.ts:304-309`): dead code; plan 002 deletes it in passing rather than a standalone plan.
- Live third-party keys in local `.env` files reported as HIGH-risk exposure: verified never committed and gitignored; reduced to the plan-006 hygiene work (scanner) rather than emergency rotation.

## Direction notes (options for the maintainer, not planned)

- The friend-recommendations input to review sessions is stubbed (`$lang.review.index.tsx:118-124`, "haven't built this feature yet") — plan 007 moves the stub to a testable seam, making it the cheapest next social-loop feature.
- `src/plans/onboarding-first-session.md` (April 2026 draft) argues for FSRS-loop-first onboarding; nothing in this plan set conflicts with it.
- Browse/request-tagging collections (`messageTags*`) landed with full handlers but no scene coverage — worth specs before that surface ships broadly.
